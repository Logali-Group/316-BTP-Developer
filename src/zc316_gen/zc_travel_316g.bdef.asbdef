projection implementation in class ZBP_C_TRAVEL_316G unique;
strict ( 2 );
use draft;
define behavior for ZC_TRAVEL_316G alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}